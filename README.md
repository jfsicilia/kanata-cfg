# Introduction

This is my kanata configuration. The main file is `kanata.kbd`.

# Info

The folder `info` contains several mapping\*.md files that provides information
of how keys are map to functionality on every app.
