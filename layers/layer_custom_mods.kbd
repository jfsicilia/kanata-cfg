;; System (QoS)
(deflayermap (system_layer)
  _ XX
)

;; Workflow
(deflayermap (workflow_layer)
  _ XX
)

;; Refactoring
(deflayermap (refactor_layer)
  _ XX
)

;; Run
(deflayermap (run_layer)
  _ XX
)


