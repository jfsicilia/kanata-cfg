(defvar
  ;; == run =================================================================
  ;; run + spc
  action_run+spc (t! unmod_all (switch ;;@iface@
    () (push-msg "NOTIFY: Run not implemented") break
  ))
  ;; run + toggle
  action_run+toggle (t! unmod_all (switch ;;@iface@
    () (push-msg "NOTIFY: Run not implemented") break
  ))
)
