(defvar
;; == replace ==========================================================
;; replace + spc
dolphin_action_replace+spc               (push-msg "NOTIFY:Replace text in file not implemented!")
;; replace + lsft + spc
dolphin_action_replace+lsft+spc          (push-msg "NOTIFY:Replace text in files not implemented!") 
;; replace + rsft + spc
dolphin_action_replace+rsft+spc          (push-msg "NOTIFY:Replace text in files not implemented!") 
)
