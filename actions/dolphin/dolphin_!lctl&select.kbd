(defvar
;; == select ========================================================
;; select + spc
dolphin_action_!lctl&select+spc                 $dolphin_go 
;; select + lsft + spc
dolphin_action_!lctl&select+lsft+spc            (push-msg "NOTIFY:Seek and select folders not implemented!") 
;; select + rsft + spc
dolphin_action_!lctl&select+rsft+spc            (push-msg "NOTIFY:Seek and select folders not implemented!") 
)
