(defvar
  ;; apps + spc
  action_apps+spc (t! unmod_all (switch
    () RA-spc break
  ))

  action_apps+!lctl (t! unmod_all (switch
    () (cmd recent_files.sh) break
  ))

  action_apps+toggle (t! unmod_all (switch
    () M-tab break
  ))

  action_apps_walk_through_same_app (switch
    () A-` break
  )

  action_apps+0 (t! unmod_all (switch 
    () (push-msg "APP:chromeMisc") break
  ))
  action_apps+1 (t! unmod_all (switch 
    () (push-msg "APP:chromeMgmt") break
  ))
  action_apps+2 (t! unmod_all (switch 
    () (push-msg "APP:chromeCloud") break
  ))
  action_apps+3 (t! unmod_all (switch 
    () (push-msg "APP:chromeAI") break
  ))
  action_apps+4 (t! unmod_all (switch 
    () (push-msg "APP:chromeBanks") break
  ))
  action_apps+5 (t! unmod_all (switch 
    () (push-msg "APP:chromeMisc1") break
  ))
  action_apps+6 (t! unmod_all (switch 
    () (push-msg "APP:chromeMisc2") break
  ))
  action_apps+7 (t! unmod_all (switch 
    () (push-msg "APP:chromeMisc3") break
  ))
  action_apps+8 (t! unmod_all (switch 
    () (push-msg "APP:chromeMisc4") break
  ))
  action_apps+9 (t! unmod_all (switch 
    () (push-msg "APP:chromeColab") break
  ))

  action_apps+a (t! unmod_all (switch 
    () (push-msg "APP:") break
  ))
  action_apps+b (t! unmod_all (switch
    () (push-msg "APP:") break
  ))
  action_apps+c (t! unmod_all (switch
    () (push-msg "APP:claude_code") break
  ))
  action_apps+d (t! unmod_all (switch 
    () (push-msg "APP:") break
  ))
  action_apps+e (t! unmod_all (switch 
    () (push-msg "APP:fileexplorer") break
  ))
  action_apps+f (t! unmod_all (switch
    () (push-msg "APP:firefox") break
  ))
  action_apps+g (t! unmod_all (switch 
    () (push-msg "APP:") break
  ))
  action_apps+h (t! unmod_all (switch
    () (push-msg "APP:") break
  ))
  action_apps+i (t! unmod_all (switch
    () (push-msg "APP:") break
  ))
  action_apps+j (t! unmod_all (switch
    () (push-msg "APP:") break
  ))
  action_apps+k (t! unmod_all (switch
    () (push-msg "APP:keepassxc") break
  ))
  action_apps+l (t! unmod_all (switch
    () (push-msg "APP:") break
  ))
  action_apps+m (t! unmod_all (switch
    () (push-msg "APP:chrome") break
  ))
  action_apps+n (t! unmod_all (switch
    () (push-msg "APP:") break
  ))
  action_apps+o (t! unmod_all (switch 
    () (push-msg "APP:obsidian") break
  ))
  action_apps+p (t! unmod_all (switch
    () (push-msg "APP:") break
  ))
  action_apps+q (t! unmod_all (switch
    () (push-msg "APP:") break
  ))
  action_apps+r (t! unmod_all (switch
    () (push-msg "APP:") break
  ))
  action_apps+s (t! unmod_all (switch
    () (push-msg "APP:sketchup") break
  ))
  action_apps+t (t! unmod_all (switch
    () (push-msg "APP:terminal") break
  ))
  action_apps+u (t! unmod_all (switch
    () (push-msg "APP:") break
  ))
  action_apps+v (t! unmod_all (switch 
    () (push-msg "APP:nvim") break
  ))
  action_apps+w (t! unmod_all (switch 
    () (push-msg "APP:zapzap") break
  ))
  action_apps+x (t! unmod_all (switch
    () (push-msg "APP:calc") break
  ))
  action_apps+y (t! unmod_all (switch
    () (push-msg "APP:") break
  ))
  action_apps+z (t! unmod_all (switch
    () (push-msg "APP:") break
  ))

  action_apps+lsft+0 (t! unmod_all (switch 
    () (push-msg "APP:firefoxMisc") break
  ))
  action_apps+lsft+1 (t! unmod_all (switch 
    () (push-msg "APP:firefoxMgmt") break
  ))
  action_apps+lsft+2 (t! unmod_all (switch 
    () (push-msg "APP:firefoxCloud") break
  ))
  action_apps+lsft+3 (t! unmod_all (switch 
    () (push-msg "APP:firefoxAI") break
  ))
  action_apps+lsft+4 (t! unmod_all (switch 
    () (push-msg "APP:firefoxBanks") break
  ))
  action_apps+lsft+5 (t! unmod_all (switch 
    () (push-msg "APP:firefoxMisc") break
  ))
  action_apps+lsft+6 (t! unmod_all (switch 
    () (push-msg "APP:") break
  ))
  action_apps+lsft+7 (t! unmod_all (switch 
    () (push-msg "APP:") break
  ))
  action_apps+lsft+8 (t! unmod_all (switch 
    () (push-msg "APP:") break
  ))
  action_apps+lsft+9 (t! unmod_all (switch 
    () (push-msg "APP:firefoxColab") break
  ))

  action_apps+lsft+a (t! unmod_all (switch 
    () (push-msg "APP:") break
  ))
  action_apps+lsft+b (t! unmod_all (switch
    () (push-msg "APP:") break
  ))
  action_apps+lsft+c (t! unmod_all (switch
    () (push-msg "APP:") break
  ))
  action_apps+lsft+d (t! unmod_all (switch 
    () (push-msg "APP:") break
  ))
  action_apps+lsft+e (t! unmod_all (switch 
    () (push-msg "APP:") break
  ))
  action_apps+lsft+f (t! unmod_all (switch
    () (push-msg "APP:fusion360") break
  ))
  action_apps+lsft+g (t! unmod_all (switch 
    () (push-msg "APP:") break
  ))
  action_apps+lsft+h (t! unmod_all (switch
    () (push-msg "APP:") break
  ))
  action_apps+lsft+i (t! unmod_all (switch
    () (push-msg "APP:") break
  ))
  action_apps+lsft+j (t! unmod_all (switch
    () (push-msg "APP:") break
  ))
  action_apps+lsft+k (t! unmod_all (switch
    () (push-msg "APP:okular") break
  ))
  action_apps+lsft+l (t! unmod_all (switch
    () (push-msg "APP:") break
  ))
  action_apps+lsft+m (t! unmod_all (switch
    () (push-msg "APP:") break
  ))
  action_apps+lsft+n (t! unmod_all (switch
    () (push-msg "APP:") break
  ))
  action_apps+lsft+o (t! unmod_all (switch 
    () (push-msg "APP:onlyoffice") break
  ))
  action_apps+lsft+p (t! unmod_all (switch
    () (push-msg "APP:") break
  ))
  action_apps+lsft+q (t! unmod_all (switch
    () (push-msg "APP:") break
  ))
  action_apps+lsft+r (t! unmod_all (switch
    () (push-msg "APP:") break
  ))
  action_apps+lsft+s (t! unmod_all (switch
    () (push-msg "APP:") break
  ))
  action_apps+lsft+t (t! unmod_all (switch
    () (push-msg "APP:telegram") break
  ))
  action_apps+lsft+lsft+t (t! unmod_all (switch
    () (push-msg "APP:") break
  ))
  action_apps+lsft+rsft+t (t! unmod_all (switch
    () (push-msg "APP:") break
  ))
  action_apps+lsft+u (t! unmod_all (switch
    () (push-msg "APP:") break
  ))
  action_apps+lsft+v (t! unmod_all (switch 
    () (push-msg "APP:") break
  ))
  action_apps+lsft+w (t! unmod_all (switch 
    () (push-msg "APP:") break
  ))
  action_apps+lsft+x (t! unmod_all (switch
    () (push-msg "APP:") break
  ))
  action_apps+lsft+y (t! unmod_all (switch
    () (push-msg "APP:") break
  ))
  action_apps+lsft+z (t! unmod_all (switch
    () (push-msg "APP:") break
  ))


  action_apps+rsft+a (t! unmod_all (switch 
    () (push-msg "APP:") break
  ))
  action_apps+rsft+b (t! unmod_all (switch
    () (push-msg "APP:") break
  ))
  action_apps+rsft+c (t! unmod_all (switch
    () (push-msg "APP:") break
  ))
  action_apps+rsft+d (t! unmod_all (switch 
    () (push-msg "APP:") break
  ))
  action_apps+rsft+e (t! unmod_all (switch 
    () (push-msg "APP:") break
  ))
  action_apps+rsft+f (t! unmod_all (switch
    () (push-msg "APP:") break
  ))
  action_apps+rsft+g (t! unmod_all (switch 
    () (push-msg "APP:") break
  ))
  action_apps+rsft+h (t! unmod_all (switch
    () (push-msg "APP:") break
  ))
  action_apps+rsft+i (t! unmod_all (switch
    () (push-msg "APP:") break
  ))
  action_apps+rsft+j (t! unmod_all (switch
    () (push-msg "APP:") break
  ))
  action_apps+rsft+k (t! unmod_all (switch
    () (push-msg "APP:") break
  ))
  action_apps+rsft+l (t! unmod_all (switch
    () (push-msg "APP:") break
  ))
  action_apps+rsft+m (t! unmod_all (switch
    () (push-msg "APP:") break
  ))
  action_apps+rsft+n (t! unmod_all (switch
    () (push-msg "APP:") break
  ))
  action_apps+rsft+o (t! unmod_all (switch 
    () (push-msg "APP:orcaslicer") break
  ))
  action_apps+rsft+p (t! unmod_all (switch
    () (push-msg "APP:") break
  ))
  action_apps+rsft+q (t! unmod_all (switch
    () (push-msg "APP:") break
  ))
  action_apps+rsft+r (t! unmod_all (switch
    () (push-msg "APP:") break
  ))
  action_apps+rsft+s (t! unmod_all (switch
    () (push-msg "APP:") break
  ))
  action_apps+rsft+t (t! unmod_all (switch
    () (push-msg "APP:") break
  ))
  action_apps+rsft+lsft+t (t! unmod_all (switch
    () (push-msg "APP:") break
  ))
  action_apps+rsft+rsft+t (t! unmod_all (switch
    () (push-msg "APP:") break
  ))
  action_apps+rsft+u (t! unmod_all (switch
    () (push-msg "APP:") break
  ))
  action_apps+rsft+v (t! unmod_all (switch 
    () (push-msg "APP:") break
  ))
  action_apps+rsft+w (t! unmod_all (switch 
    () (push-msg "APP:") break
  ))
  action_apps+rsft+x (t! unmod_all (switch
    () (push-msg "APP:") break
  ))
  action_apps+rsft+y (t! unmod_all (switch
    () (push-msg "APP:") break
  ))
  action_apps+rsft+z (t! unmod_all (switch
    () (push-msg "APP:") break
  ))
)
