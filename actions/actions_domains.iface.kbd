(defvar
  clipboard_history M-v
  ai                (push-msg "NOTIFY: AI")
)
(defvar

  ;; domainA + A
  action_domainA+A (t! unmod_all (switch ;;@iface@
    ((input virtual vk_nvim)) $nvim_action_domainA+A break
    () (push-msg "NOTIFY: Actions not implemented") break
  ))

  ;; domainB + B
  action_domainB+B (t! unmod_all (switch ;;@iface@
    ((input virtual vk_obsidian)) $obsidian_action_domainB+B break
    ((input virtual vk_nvim)) $nvim_action_domainB+B break
    () (push-msg "NOTIFY: Bookmarks not implemented") break
  ))

  ;; domainC + C
  action_domainC+C (t! unmod_all (switch ;;@iface@
    ((input virtual vk_obsidian)) $obsidian_action_domainC+C break
    () (push-msg "NOTIFY: Calendar not implemented") break
  ))
  ;; domainC + D
  action_domainC+D (t! unmod_all (switch ;;@iface@
    ((input virtual vk_obsidian)) $obsidian_action_domainC+D break
    () (push-msg "NOTIFY: Calendar (Day) not implemented") break
  ))
  ;; domainC + Y
  action_domainC+Y (t! unmod_all (switch ;;@iface@
    ((input virtual vk_obsidian)) $obsidian_action_domainC+Y break
    () (push-msg "NOTIFY: Calendar (Daily) not implemented") break
  ))

  ;; domainD + D
  action_domainD+D (t! unmod_all (switch ;;@iface@
    ((input virtual vk_nvim)) $nvim_action_domainD+D break
    () (push-msg "NOTIFY: Definitions not implemented") break
  ))
  ;; domainD + S
  action_domainD+S (t! unmod_all (switch ;;@ifaceS
    () (push-msg "NOTIFY: Definition (Source) not implemented") break
  ))
 
  ;; domainE + E
  action_domainE+E (t! unmod_all (switch ;;@iface@
    ((input virtual vk_obsidian)) $obsidian_action_domainE+E break
    ((input virtual vk_dolphin)) $dolphin_action_domainE+E break
    ((input virtual vk_nvim)) $nvim_action_domainE+E break
    () (push-msg "NOTIFY: Explorer not implemented") break
  ))
  ;; domainE + R
  action_domainE+R (t! unmod_all (switch ;;@iface@
    ((input virtual vk_obsidian)) $obsidian_action_domainE+R break
    () (push-msg "NOTIFY: Explorer (Recents) not implemented") break
  ))
  ;; domainE + F
  action_domainE+F (t! unmod_all (switch ;;@iface@
    ((input virtual vk_obsidian)) $obsidian_action_domainE+F break
    () (push-msg "NOTIFY: Explorer (Find/Focus) not implemented") break
  ))

  ;; domainF + F
  action_domainF+F (t! unmod_all (switch ;;@iface@
    () (push-msg "NOTIFY: Filters not implemented") break
  ))

  ;; domainG + G
  action_domainG+G (t! unmod_all (switch ;;@iface@
    ((input virtual vk_foot)) $foot_action_domainG+G break
    ((input virtual vk_nvim)) $nvim_action_domainG+G break
    () (push-msg "NOTIFY: Git not implemented") break
  ))
  ;; domainG + L
  action_domainG+L (t! unmod_all (switch ;;@iface@
    ((input virtual vk_foot)) $foot_action_domainG+L break
    ((input virtual vk_nvim)) $nvim_action_domainG+L break
    () (push-msg "NOTIFY: Git (Lazygit) not implemented") break
  ))

  ;; domainH + H
  action_domainH+H (t! unmod_all (switch ;;@iface@
    () (push-msg "NOTIFY: Hyperlinks not implemented") break
  ))
  ;; domainH + O
  action_domainH+O (t! unmod_all (switch ;;@iface@
    ((input virtual vk_obsidian)) $obsidian_action_domainH+O break
    () (push-msg "NOTIFY: OutLine not implemented") break
  ))

  ;; domainI + I
  action_domainI+I (t! unmod_all (switch ;;@iface@
    ((input virtual vk_dolphin)) $dolphin_action_domainI+I break
    () (push-msg "NOTIFY: Info not implemented") break
  ))

  ;; domainJ + J
  action_domainJ+J (t! unmod_all (switch ;;@iface@
    ((input virtual vk_nvim)) $nvim_action_domainJ+J break
    () (push-msg "NOTIFY: Jumps not implemented") break
  ))

  ;; domainK + K
  action_domainK+K (t! unmod_all (switch ;;@iface@
    ((input virtual vk_chrome)) $chrome_action_domainK+K break
    ((input virtual vk_dolphin)) $dolphin_action_domainK+K break
    ((input virtual vk_firefox)) $firefox_action_domainK+K break
    ((input virtual vk_colab)) $colab_action_domainK+K break
    ((input virtual vk_nvim)) $nvim_action_domainK+K break
    () (push-msg "NOTIFY: Keymaps not implemented") break
  ))

  ;; domainL + L
  action_domainL+L (t! unmod_all (switch ;;@iface@
    ((input virtual vk_nvim)) $nvim_action_domainL+L break
    () (push-msg "NOTIFY: Locations not implemented") break
  ))

  ;; domainM + M
  action_domainM+M (t! unmod_all (switch ;;@iface@
    ;; () (push-msg "NOTIFY: Memory not implemented") break
    ()                      $clipboard_history break  ;; Clipboard history
  ))

  ;; domainN + N
  action_domainN+N (t! unmod_all (switch ;;@iface@
    () (push-msg "NOTIFY: notebooks not implemented") break
  ))

  ;; domainO + O
  action_domainO+O (t! unmod_all (switch ;;@iface@
    () (push-msg "NOTIFY: Output not implemented") break
  ))

  ;; domainP + P
  action_domainP+P (t! unmod_all (switch ;;@iface@
    ((input virtual vk_obsidian)) $obsidian_action_domainP+P break
    () (push-msg "NOTIFY: Properties not implemented") break
  ))
  ;; domainP + A
  action_domainP+A (t! unmod_all (switch ;;@iface@
    ((input virtual vk_obsidian)) $obsidian_action_domainP+A break
    () (push-msg "NOTIFY: Properties All not implemented") break
  ))
  
  ;; domainQ + Q
  action_domainQ+Q (t! unmod_all (switch ;;@iface@
    ((input virtual vk_nvim)) $nvim_action_domainQ+Q break
    () (push-msg "NOTIFY: Quickfix not implemented") break
  ))

  ;; domainR + R
  action_domainR+R (t! unmod_all (switch ;;@iface@
    ((input virtual vk_nvim)) $nvim_action_domainR+R break
    () (push-msg "NOTIFY: References not implemented") break
  ))
  ;; domainR + I
  action_domainR+I (t! unmod_all (switch ;;@iface@
    ((input virtual vk_nvim)) $nvim_action_domainR+I break
    () (push-msg "NOTIFY: References (Incoming) not implemented") break
  ))
  ;; domainR + O
  action_domainR+O (t! unmod_all (switch ;;@iface@
    ((input virtual vk_nvim)) $nvim_action_domainR+O break
    () (push-msg "NOTIFY: References (Outgoing) not implemented") break
  ))
  ;; domainR + S
  action_domainR+S (t! unmod_all (switch ;;@ifaceS
    () (push-msg "NOTIFY: Source not implemented") break
  ))

  ;; domainS + S
  action_domainS+S (t! unmod_all (switch ;;@iface@
    ((input virtual vk_obsidian)) $obsidian_action_domainS+S break
    ((input virtual vk_nvim)) $nvim_action_domainS+S break
    () (push-msg "NOTIFY: Symbols not implemented") break
  ))
  ;; domainS + W
  action_domainS+W (t! unmod_all (switch ;;@iface@
    ((input virtual vk_nvim)) $nvim_action_domainS+W break
    () (push-msg "NOTIFY: Symbols (Workspace) not implemented") break
  ))

  ;; domainT + T
  action_domainT+T (t! unmod_all (switch ;;@iface@
    ((input virtual vk_dolphin)) $dolphin_action_domainT+T break
    ((input virtual vk_nvim)) $nvim_action_domainT+T break
    () (push-msg "NOTIFY: Terminal not implemented") break
  ))
  ;; domainT + toggle
  action_domainT+toggle (t! unmod_all (switch ;;@iface@
    ((input virtual vk_nvim)) $nvim_action_domainT+toggle break
    () (push-msg "NOTIFY: Toggle Terminal not implemented") break
  ))

  ;; domainU + U
  action_domainU+U (t! unmod_all (switch ;;@iface@
    () (push-msg "NOTIFY: UI not implemented") break
  ))
  ;; domainU + G
  action_domainU+G (t! unmod_all (switch ;;@iface@
    ((input virtual vk_obsidian)) $obsidian_action_domainU+G break
    () (push-msg "NOTIFY: UI Graph not implemented") break
  ))

  ;; domainV + V
  action_domainV+V (t! unmod_all (switch ;;@iface@
    () (push-msg "NOTIFY: Virtualization not implemented") break
  ))

  ;; domainW + W
  action_domainW+W (t! unmod_all (switch ;;@iface@
    () (push-msg "NOTIFY: Warnings not implemented") break
  ))

  ;; domainX + X
  action_domainX+X (t! unmod_all (switch ;;@iface@
    () (push-msg "NOTIFY: Extensions not implemented") break
  ))

  ;; domainY + Y
  action_domainY+Y (t! unmod_all (switch ;;@iface@
    ((input virtual vk_nvim)) $nvim_action_domainY+Y break
    () (push-msg "NOTIFY: History not implemented") break
  ))
  ;; domainY + ;
  action_domainY+; (t! unmod_all (switch ;;@iface@
    ((input virtual vk_nvim)) $nvim_action_domainY+; break
    () (push-msg "NOTIFY: History (commands) not implemented") break
  )) ;;""
  ;; domainY + search 
  action_domainY+search (t! unmod_all (switch ;;@iface@
    ((input virtual vk_nvim)) $nvim_action_domainY+search break
    () (push-msg "NOTIFY: History search not implemented") break
  ))

  ;; domainZ + Z
  action_domainZ+Z (t! unmod_all (switch ;;@iface@
    () (push-msg "NOTIFY: Tests not implemented") break
  ))

  ;; domain` + `
  action_domain`+` (t! unmod_all (switch ;;@iface@
    () (push-msg "NOTIFY: mod` not implemented") break
  ))

  ;; domain- + -
  action_domain-+- (t! unmod_all (switch ;;@iface@
    () (push-msg "NOTIFY: mod- not implemented") break
  ))

  ;; domain= + =
  action_domain=+= (t! unmod_all (switch ;;@iface@
    () (push-msg "NOTIFY: Format not implemented") break
  )) ;;""

  ;; domain[ + [
  action_domain[+[ (t! unmod_all (switch ;;@iface@ 
    ((input virtual vk_obsidian)) $obsidian_action_domain[+[ break
    () (push-msg "NOTIFY: Backlinks not implemented") break
  )) ;;""

  ;; domain] + ]
  action_domain]+] (t! unmod_all (switch ;;@iface@
    ((input virtual vk_obsidian)) $obsidian_action_domain]+] break
    () (push-msg "NOTIFY: Outlinks not implemented") break
  )) ;;""
  ;; domain] + A
  action_domain]+A (t! unmod_all (switch ;;@iface@
    ((input virtual vk_obsidian)) $obsidian_action_domain]+A break
    () (push-msg "NOTIFY: Outlinks All not implemented") break
  )) ;;""

  ;; domain\ + \
  action_domain\+\ (t! unmod_all (switch ;;@iface@
    ((input virtual vk_foot)) $foot_action_domain\+\ break
    ((input virtual vk_nvim)) $nvim_action_domain\+\ break
    () (push-msg "NOTIFY: Toggle compare files not implemented") break
  )) ;;""
  ;; domain\ + lsft + \
  action_domain\+lsft+\ (t! unmod_all (switch ;;@iface@
    ((input virtual vk_foot)) $foot_action_domain\+lsft+\ break
    () (push-msg "NOTIFY: Toggle compare folders not implemented") break
  )) ;;""
  ;; domain\ + j
  action_domain\+j (t! unmod_all (switch ;;@iface@
    ((input virtual vk_nvim)) $nvim_action_domain\+j break
    () (push-msg "NOTIFY: Compare next not implemented") break
  )) ;;""
  ;; domain\ + k
  action_domain\+k (t! unmod_all (switch ;;@iface@
    ((input virtual vk_nvim)) $nvim_action_domain\+k break
    () (push-msg "NOTIFY: Compare before not implemented") break
  )) ;;""
  ;; domain\ + o
  action_domain\+o (t! unmod_all (switch ;;@iface@
    ((input virtual vk_nvim)) $nvim_action_domain\+o break
    () (push-msg "NOTIFY: Compare obtain not implemented") break
  )) ;;""
  ;; domain\ + p
  action_domain\+p (t! unmod_all (switch ;;@iface@
    ((input virtual vk_nvim)) $nvim_action_domain\+p break
    () (push-msg "NOTIFY: Compare push not implemented") break
  )) ;;""

  ;; domain; + ;
  action_domain;+; (t! unmod_all (switch ;;@iface@
    ((input virtual vk_obsidian)) $obsidian_action_domain;+; break
    ((input virtual vk_colab)) $colab_action_domain;+; break
    ((input virtual vk_nvim)) $nvim_action_domain;+; break
    ((input virtual vk_tmux)) $tmux_action_domain;+; break
    () (push-msg "NOTIFY: Commands not implemented") break
  )) ;;""

  ;; domain' + '
  action_domain'+' (t! unmod_all (switch ;;@iface@
    ((input virtual vk_nvim)) $nvim_action_domain'+' break
    () (push-msg "NOTIFY: Registries not implemented") break
  ))

  ;; domain, + ,
  action_domain,+, (t! unmod_all (switch ;;@iface@
    ((input virtual vk_chrome)) $chrome_action_domain,+, break
    ((input virtual vk_zapzap)) $zapzap_action_domain,+, break
    ((input virtual vk_obsidian)) $obsidian_action_domain,+, break
    ((input virtual vk_dolphin)) $dolphin_action_domain,+, break
    ((input virtual vk_firefox)) $firefox_action_domain,+, break
    ((input virtual vk_nvim)) $nvim_action_domain,+, break
    () (push-msg "NOTIFY: Settings not implemented") break
  ))

  ;; domain. + .
  action_domain.+. (t! unmod_all (switch ;;@iface@
    ((input virtual vk_dolphin)) $dolphin_action_domain.+. break
    ((input virtual vk_nvim)) $nvim_action_domain.+. break
    () (push-msg "NOTIFY: Tags not implemented") break
  ))

  ;; domain/ + / 
  action_domain/+/ (t! unmod_all (switch ;;@iface@
    ((input virtual vk_dolphin)) $dolphin_action_domain/+/ break
    ((input virtual vk_nvim)) $nvim_action_domain/+/ break
    () (push-msg "NOTIFY: Help not implemented") break
  )) ;;""

  ;; domain0 + 0
  action_domain0+0 (t! unmod_all (switch ;;@iface@
    () (push-msg "NOTIFY: mod0 not implemented") break
  ))

  ;; domain1 + 1
  action_domain1+1 (t! unmod_all (switch ;;@iface@
    () (push-msg "NOTIFY: mod1 not implemented") break
  ))

  ;; domain2 + 2
  action_domain2+2 (t! unmod_all (switch ;;@iface@
    () (push-msg "NOTIFY: mod2 not implemented") break
  ))

  ;; domain3 + 3
  action_domain3+3 (t! unmod_all (switch ;;@iface@
    () (push-msg "NOTIFY: mod3 not implemented") break
  ))

  ;; domain4 + 4
  action_domain4+4 (t! unmod_all (switch ;;@iface@
    () (push-msg "NOTIFY: mod4 not implemented") break
  ))

  ;; domain5 + 5
  action_domain5+5 (t! unmod_all (switch ;;@iface@
    () (push-msg "NOTIFY: mod5 not implemented") break
  ))

  ;; domain6 + 6
  action_domain6+6 (t! unmod_all (switch ;;@iface@
    () (push-msg "NOTIFY: mod6 not implemented") break
  ))

  ;; domain7 + 7
  action_domain7+7 (t! unmod_all (switch ;;@iface@
    () (push-msg "NOTIFY: mod7 not implemented") break
  ))

  ;; domain8 + 8
  action_domain8+8 (t! unmod_all (switch ;;@iface@
    () (push-msg "NOTIFY: mod8 not implemented") break
  ))

  ;; domain9 + 9
  action_domain9+9 (t! unmod_all (switch ;;@iface@
    () (push-msg "NOTIFY: mod9 not implemented") break
  ))

  ;; omni + omni
  action_omni+omni (t! unmod_all (switch ;;@iface@
    () $ai break
  ))
 

)
