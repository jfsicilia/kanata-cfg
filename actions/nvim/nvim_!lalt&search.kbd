(defvar
;; == search ============================================================
;; search + spc
;; nvim_action_search+spc    `{esc}:Telescope current_buffer_fuzzy_find{ret}`
nvim_action_search+spc        $nvim_search_in_buffer
;; search + lsft + spc
;; nvim_action_search+lsft+spc     `{esc}:Telescope live_grep{ret}`
nvim_action_search+lsft+spc   $nvim_search_in_workspace 
)
