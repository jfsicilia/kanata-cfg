# Physical mods (!lalt) - nvim (actions/nvim/nvim_physical_mods.kbd)
**search + spc**        -- Search In Buffer ([esc] :Telescope current_buffer_fuzzy_find [ret])
**search + lsft + spc** -- Search In Workspace ([esc] :Telescope live_grep [ret])
