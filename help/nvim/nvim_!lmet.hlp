# Physical mods (!lmet) - nvim (actions/nvim/nvim_physical_mods.kbd)
**!lmet + l**            -- Lock User Session
**replace + spc**        -- Replace In Buffer
**replace + lsft + spc** -- Replace In Workspace
